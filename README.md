# CV Screening Pipeline

An automated CV screening pipeline that extracts structured data from unstructured CVs,
ranks candidates against a role, and keeps a human recruiter in control of every decision.

Built on **synthetic data only** — see [Why synthetic data](#why-synthetic-data).

> **Status:** 🚧 In progress. This repository is being built in public, module by module.
> See [Roadmap](#roadmap) for what is done and what is next.

---

## Why this project

Recruitment screening is a high-volume, repetitive task that looks like an obvious
automation target — and it is also one of the few automation targets that European
regulators single out by name.

Under the **EU AI Act, Annex III**, AI systems used to *filter job applications and
evaluate candidates* are classified as **high-risk**. That changes what "done" means:
a screening pipeline is not finished when it produces a ranking. It is finished when
the ranking is explainable, auditable, testable for bias, and overridable by a human.

This project treats those obligations as engineering requirements, not paperwork.

**Regulatory timeline (as of August 2026):**

| Obligation | Applies from |
|---|---|
| Article 50 transparency duties | **2 August 2026** — in force |
| Full high-risk obligations (Annex III stand-alone systems) | **2 December 2027** (moved by the Digital Omnibus amendment) |

Penalties for deployer non-compliance reach **€15M or 3% of global annual turnover**,
whichever is higher, and can reach deployers established outside the EU.

*This is an engineering project, not legal advice.*

---

## Architecture

```
  Synthetic CVs (.txt)
          │
          ▼
  [1] Parse / normalise          ── handles messy, inconsistent CV formats
          │
          ▼
  [2] LLM structured extraction  ── JSON Schema enforced, not free-form text
          │
          ▼
  [3] Explainable scoring        ── every point traceable to a requirement
          │
          ▼
  [4] Human-in-the-loop review   ── recruiter can override or reverse any result
          │
          ▼
  [5] Audit trail                ── who decided what, when, and on what evidence
          │
          ▼
  Metrics: extraction accuracy · ranking agreement · bias parity · throughput
```

## Why synthetic data

Real CVs are personal data under GDPR, and employer data is confidential. Every CV in
this repository is generated by [`src/generate_synthetic_cvs.py`](src/generate_synthetic_cvs.py).

Synthetic data buys two things beyond safety:

1. **Ground truth.** The generator writes the correct answer alongside each CV, so
   extraction accuracy can be *measured* rather than eyeballed.
2. **Controlled bias tests.** The generator emits matched pairs — CVs with identical
   qualifications but different demographic signals — so the scoring stage can be
   tested for disparate treatment.

Generation is seeded, so the dataset is reproducible.

---

## Getting started

```bash
# 1. Generate the synthetic dataset (no dependencies required)
python src/generate_synthetic_cvs.py --count 60 --seed 42

# 2. Inspect what was produced
#    data/synthetic/cv_0001.txt    <- unstructured CV, the pipeline input
#    data/synthetic/cv_0001.json   <- ground truth, what extraction should return
#    data/synthetic/_manifest.json <- dataset index + bias-test pairs
```

## Repository layout

```
cv-screening-pipeline/
├── data/
│   ├── samples/         3 committed examples, so the repo reads without running anything
│   ├── synthetic/       Full generated set (git-ignored — reproducible from the seed)
│   └── role_*.json      The role definition candidates are scored against
├── docs/                Problem statement, decisions, compliance notes
├── src/                 Pipeline source
└── tests/               Tests, including failure cases
```

---

## Roadmap

- [x] Synthetic dataset generator with ground truth and bias pairs
- [ ] Problem statement and success criteria
- [ ] CV parsing and normalisation
- [ ] LLM structured extraction against a JSON Schema
- [ ] Evaluation harness — extraction accuracy vs. ground truth
- [ ] Explainable scoring and ranking
- [ ] Human-in-the-loop override flow
- [ ] Bias parity testing across matched pairs
- [ ] Audit trail and decision logging
- [ ] Failure-path tests and retry / idempotency
- [ ] Metrics dashboard and results write-up

---

## Documentation

- [Problem statement](docs/problem-statement.md) — the problem, success criteria, risks, and why an LLM
