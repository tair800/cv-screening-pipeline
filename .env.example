# Copy to .env and fill in LLM_API_KEY. .env is git-ignored and must never be committed.
# Any OpenAI-compatible endpoint works: a gateway, a hosted provider, a local runtime.

LLM_BASE_URL=http://localhost:20128/v1
LLM_API_KEY=
LLM_MODEL=auto/best-free
